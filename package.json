{
  "name": "yt-dl",
  "version": "1.0.0",
  "description": "This is AWS Lambda function, it will generate a website, process youtube url and show video info.",
  "main": "index.js",
  "scripts": {
    "dev": "node test.js",
    "build": "zip -r ${PWD##*/}_$(date +%s).zip . -x *.git* -x *.zip"
  },
  "author": "Tony Pai",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "pug": "^2.0.3",
    "youtube-dl": "^2.0.0"
  }
}
